---
id: intro screen for petition
question: Petition
continue button field: intro_screen


---
include:
  - county.yml
---
id: additional information about attorney 
question: Additional attorney about the ${ petitioner.attorney.name_full() }, attorney helping ${ petitioner.name_full() }
fields:
  - Email for ${ petitioner.attorney.name_full() }: petitioner.attorney.email
    required: False
  - Phone for ${ petitioner.attorney.name_full() }: petitioner.attorney.phone
    required: False
continue button field: additional_attorney_infomation
---
## MARRIAGE

id: broken marriage
question: Status of marriage
subquestion: |
  This interview is only designed to help with dissolutions. The court will only grant a dissolution if
  - the marriage is irretrievably broken, and
  - the marriage can not be preserved.
fields:
  - Is the marriage irretrievable broken?: marriage.is_broken
    datatype: yesnoradio
    default: True
  - Can the marriage be preserved?: marriage.can_be_preserved
    datatype: yesnoradio
    default: False

---
id: Family support order
question: Order by the Family Support Division
fields:
  - Have any orders pertaining to any of the children been entered by the Family Support Division?: family_support_order
    datatype: yesnoradio
  - Upload the order to attach to the petition: family_support_order_file
    datatype: file
    required: False
    show if:
      variable: family_support_order
      is: True


---
## CHILDREN  
---
if: not children[i].birth_certificate[users[0].name.full()] or not not children[i].birth_certificate[other_parties[0].name.full()]
id: adoption question
question: Is ${ children[i].name.full() } adopted by one or both parents?
fields:
  - Is ${ children[i].name.full() } adopted by ${ users[0].name.full() }?: children[i].adopted_by_user
    datatype: yesnoradio
    show if:
      code: |
        not children[i].birth_certificate[users[0].name.full()] 
  - Is ${ children[i].name.full() } adopted by ${ other_parties[0].name.full() }?: children[i].adopted_by_other_party
    datatype: yesnoradio
    show if:
      code: |
        not children[i].birth_certificate[other_parties[0].name.full()] 
continue button field: adopted_question
---
id: adoption question simple
question: Is ${ children[i].name.full() } adopted by one or both parents?
fields:
  - no label: children[i].adopted
    datatype: yesnoradio
---
id: Petition for Dissolution of Marriage object
objects:
  - petition_attachment: ALDocument.using(title="Petition for Dissolution of Marriage", filename="petition", has_addendum=False, )
---
id: Petition for Dissolution of Marriage enabled
code: |
  petition_attachment.enabled = True
---
id: Petition for Dissolution of Marriage attachment
attachment:
  name: Petition for Dissolution of Marriage
  filename: petition
  variable name: petition_attachment[i]
  skip undefined: True
  pdf template file: petition.pdf
  editable: False
  fields:
      - "countyList": ${ case.county } 
      - "petitionersFirstName1": ${ petitioner.name.first } 
      - "petitionersMiddleName1": ${ petitioner.name.middle } 
      - "petitionersLastName1": ${ petitioner.name.last } 
      - "petitionersSuffixJrSrIII1": ${ petitioner.name.suffix } 
      - "respondentsFirstName1": ${ respondent.name.first } 
      - "respondentsMiddleName1": ${ respondent.name.middle } 
      - "respondentsLastName1": ${ respondent.name.last } 
      - "respondentsSuffixJrSrIII1": ${ respondent.name.suffix } 
      - "petitionersFirstName2": ${ petitioner.name.first } 
      - "petitionersMiddleName2": ${ petitioner.name.middle } 
      - "petitionersLastName2": ${ petitioner.name.last } 
      - "petitionersSuffixJrSrIII2": ${ petitioner.name.suffix } 
      - "respondentsFirstName2": ${ respondent.name.first } 
      - "respondentsMiddleName2": ${ respondent.name.middle } 
      - "respondentsLastName2": ${ respondent.name.last } 
      - "respondentsSuffixJrSrIII2": ${ respondent.name.suffix } 
      - "petitionersFirstName3": ${ petitioner.name.first } 
      - "petitionersMiddleName3": ${ petitioner.name.middle } 
      - "petitionersLastName3": ${ petitioner.name.last } 
      - "petitionersSuffixJrSrIII3": ${ petitioner.name.suffix } 
      - "howManyPetitions1": ${ previous_petitions == 0 } 
      - "howManyPetitions2": ${ previous_petitions == 1 } 
      - "howManyPetitions3": ${ previous_petitions > 1 } 
      - "petitionersStreet": ${ petitioner.address.address } 
      - "petitionersCity": ${ petitioner.address.city } 
      - "petitionersState": ${ state_name(petitioner.address.state) } 
      - "petitonersZip": ${ petitioner.address.zip } 
      - "petitionersPhone": ${ phone_number_formatted(petitioner.phone_number) } 
      - "petitonersEmail": ${ petitioner.email } 
      - "petitionersSsnLastFour": ${ redact(petitioner.ssn) } 
      - "petitionerOverEighteenOrNot1": ${ petitioner.birthdate.plus(years=18) < today() } 
      - "petitionerOverEighteenOrNot2": ${ petitioner.birthdate.plus(years=18) >= today()  } 
      - "petitonersCountryOfResidenceName": ${ petitioner.address.country if petitioner.address.country != "US" else "" } 
      - "petitionersCountryOfResidence1": ${ petitioner.address.country == "US" } 
      - "petitionersCountryOfResidence2": ${ petitioner.address.country != "US" } 
      - "petitionersStateOfResidenceName": ${ state_name(petitioner.address.state) if petitioner.address.state != "MO" else "" } 
      - "petitionersStateOfResidence1": ${ petitioner.address.state == "MO" } 
      - "petitionersStateOfResidence2": ${ petitioner.address.state != "MO" } 
      - "petitionerStateOfResidenceYears": ${ petitioner.mo_length_of_residence_years } 
      - "petitionerStateOfResidenceMonths": ${ petitioner.mo_length_of_residence_months } 
      - "petitonerStateOfResidenceDays": ${ petitioner.mo_length_of_residence_days } 
      - "petitionersCountyOfResidenceName": ${ petitioner.address.county } 
      - "petitionersCountyOfResidenceYears": ${ petitioner.county_length_of_residence_years } 
      - "petitionersCountyOfResidenceMonths": ${ petitioner.county_length_of_residence_months } 
      - "petitionersCountyOfResidenceDays": ${ petitioner.county_length_of_residence_days } 
      - "petitionersEmploymentStatus1": ${ len(petitioner.jobs) > 0 } 
      - "petitionersEmploymentStatus2": ${ len(petitioner.jobs) == 0 and not petitioner.has_self_employment_income } 
      - "petitionersEmploymentStatus3": ${ len(petitioner.jobs) == 0 and petitioner.has_self_employment_income } 
      - "petitionerEmployedOrSelfEmployed": ${ showifdef('petitioner.employer.name.text', showifdef('petitioner.self_employment_description'))  } 
      - "petitionersEmploymentStreet": ${ petitioner.employer.address.address } 
      - "petitionersEmploymentCity": ${ petitioner.employer.address.city } 
      - "petitionersEmploymentState": ${ state_name(petitioner.employer.address.state) } 
      - "petitionersEmploymentZip": ${ petitioner.employer.address.zip } 
      - "petitionersMonthlyGrossIncome": ${ thousands(petitioner.monthly_gross_income) } 
      - "petitionerSupportYourself1": ${ petitioner.self_supporting } 
      - "petitionerSupportYourself2": ${ not petitioner.self_supporting } 
      - "respondentsFirstName3": ${ respondent.name.first } 
      - "respondentsLastName3": ${ respondent.name.last } 
      - "respondentsMiddleName3": ${ respondent.name.middle } 
      - "respondentsSuffixJrSrIII3": ${ respondent.name.suffix } 
      - "respondentsStreet": ${ respondent.address.address } 
      - "respondentsCity": ${ respondent.address.city } 
      - "respondentsState": ${ state_name(respondent.address.state) } 
      - "respondentsZip": ${ respondent.address.zip } 
      - "respondentsPhone": ${ respondent.email } 
      - "respondentsEmail": ${ phone_number_formatted(respondent.phone_number) } 
      - "respondentsSsnLast4": ${ redact(respondent.ssn) } 
      - "respondentOverEighteenOrNot1": ${ respondent.birthdate.plus(years=18) < today() } 
      - "respondentOverEighteenOrNot2": ${ respondent.birthdate.plus(years=18) >= today()  } 
      - "respondentsCountryOfResidenceName": ${ respondent.address.country if respondent.address.country != "US" else "" } 
      - "respondentsCountryOfResidence1": ${ respondent.address.country == "US" } 
      - "respondentsCountryOfResidence2": ${ respondent.address.country != "US" } 
      - "respondentsStateOfResidenceName": ${ state_name(respondent.address.state) if respondent.address.state != "MO" else "" } 
      - "respondentsStateOfResidence1": ${ respondent.address.state == "MO" } 
      - "respondentsStateOfResidence2": ${ respondent.address.state != "MO" } 
      - "respondentsStateOfResidenceYears": ${ respondent.mo_length_of_residence_years } 
      - "respondentsStateOfResidenceMonths": ${ respondent.mo_length_of_residence_months } 
      - "respondentsStateOfResidenceDays": ${ respondent.mo_length_of_residence_days } 
      - "respondentsCountyOfResidenceName": ${ respondent.address.county } 
      - "respondentsCountyOfResidenceYears": ${ respondent.county_length_of_residence_years } 
      - "respondentsCountyOfResidenceMonths": ${ respondent.county_length_of_residence_months } 
      - "respondentsCountyOfResidenceDays": ${ respondent.county_length_of_residence_days } 
      - "didCoupleLiveInMissouri1": ${ marriage.live_together_in_mo } 
      - "didCoupleLiveInMissouri2": ${ not marriage.live_together_in_mo } 
      - "didCoupleLiveInMissouri3": ${ other_parties[0].address.state == "MO" } 
      - "respondentsEmploymentStatus1": ${ len(respondent.jobs) > 0 } 
      - "respondentsEmploymentStatus2": ${ respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False} 
      - "respondentsEmploymentStatus3": ${ len(respondent.jobs) == 0 and respondent.has_self_employment_income } 
      - "respondentsEmploymentStatus4": ${ not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False)  } 
      - "respondentEmployedOrSelfEmployed": ${ "Unknown" if not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False) else showifdef('respondent.employer.name.text') } 
      - "respondentsEmploymentStreet": ${ "" if not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False) else respondent.employer.address.address } 
      - "respondentsEmploymentState": ${ "" if not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False) else respondent.employer.address.state } 
      - "respondentsEmploymentCity": ${ "" if not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False) else respondent.employer.address.city } 
      - "respondentsEmploymentZip": ${ "" if not len(respondent.jobs) > 0 and not respondent.has_self_employment_income and not (respondent.jobs.there_are_any == False and respondent.has_self_employment_income == False) else respondent.employer.address.zip } 
      - "respondentsMonthlyGrossIncome": ${ thousands(respondent.monthly_gross_income) } 
      - "respondentSupportThemselves1": ${ respondent.self_supporting } 
      - "respondentSupportThemselves2": ${ respondent.self_supporting == False } 
      - "respondentSupportThemselves3": ${ respondent.self_supporting == None } 
      - "respondentActiveDuty1": ${ respondent.active_duty } 
      - "respondentActiveDuty2": ${ not respondent.active_duty } 
      - "dateOfMarriage": ${ format_date(marriage.recorded_date, format='MM/dd/yyyy') } 
      - "locationMarriageLicenseState": ${ state_name(marriage.address.state) } 
      - "locationMarriageLicenseCounty": ${ nice_county_name(marriage.address) } 
      - "locationMarriageLicense1": ${ marriage.address.country == "US" } 
      - "locationMarriageLicense2": ${ marriage.address.country != "US" } 
      - "locationMarriageLicenseCountry": ${ marriage.address.country if marriage.address.country != "US" else ""  } 
      - "dateOfSeparation": ${ marriage.separation_date } 
      - "canMarriageBePreserved1": ${ marriage.can_be_preserved } 
      - "canMarriageBePreserved2": ${ not marriage.can_be_preserved } 
      - "isMarriageIrretrievablyBroken1": ${ marriage.is_broken } 
      - "isMarriageIrretrievablyBroken2": ${ not marriage.is_broken } 
      - "yesImPregnant": ${ False } 
      - "yesSpouseIsPregnant": ${ False } 
      - "noNeitherPregnant": ${ True } 
      - "numberOfChildrenAfterMarriageDate": ${  len([child for child in children if child.birthdate > marriage.recorded_date]) } 
      - "numberOfChildrenAdopted": ${ len([child for child in children if child.adopted == True]) } 
      - "numberOfChildrenTogetherBeforeDateOfMarriage": ${ len([child for child in children if child.birthdate < marriage.recorded_date]) } 
      - "numberOfChildrenBornToYouAndSomeoneOtherThanSpouseDuringMarriage": ${ 0 } 
      - "numberOfChildrenBornToSpouseWithSomeoneElseDuringMarriage": ${ 0 } 
      - "totalNumberOfChildren": ${ len(children) } 
      - "childOneFirstName": |
          % if len(children) > 0:
          ${ redact(children[0].name.first) }
          % endif
      - "childOneMiddleName": |
          % if len(children) > 0:
          ${ redact(children[0].name.middle) }
          % endif
      - "childOneLastName": |
          % if len(children) > 0:
          ${ redact(children[0].name.last) }
          % endif
      - "childOneSuffixJrSrIII": |
          % if len(children) > 0:
          ${ redact(children[0].name.suffix) }
          % endif
      - "childOneSsnLastFour": |
          % if len(children) > 0:
          ${ redact("{:.0f}".format(children[0].ssn)) }
          % endif
      - "childOneStreet": |
          % if len(children) > 0:
          ${ children[0].address.line_one() }
          % endif
      - "childOneCity": |
          % if len(children) > 0:
          ${ children[0].address.city }
          % endif
      - "childOneState": |
          % if len(children) > 0:
          ${ children[0].address.state }
          % endif
      - "childOneZip": |
          % if len(children) > 0:
          ${ children[0].address.zip }
          % endif
      - "childOneAge": |
          % if len(children) > 0:
          % if date_difference(starting=children[0].birthdate,ending=today()).years > 1:
          ${ "{:.0f}".format(date_difference(starting=children[0].birthdate,ending=today()).years) }
          % else:
          ${ date_difference(starting=children[0].birthdate, ending=today()).describe(specificity='month') }
          % endif
          % endif
      - "childOneMarriedOrNot": |
          % if len(children) > 0:
          ${ children[0].special_factors["married"] }
          % endif
      - "childOneActiveDutyOrNot": |
          % if len(children) > 0:
          ${ children[0].special_factors["active_duty"] }
          % endif
      - "childOneSelfSupportingOrNot": |
          % if len(children) > 0:
          ${ children[0].special_factors["self_supporting"] }
          % endif
      - "childOneAttendingHighSchoolOrNot": |
          % if len(children) > 0:
          ${ children[0].special_factors["high_school"] }
          % endif
      - "childOneAttendingCollegeOrVocSchool": |
          % if len(children) > 0:
          ${ children[0].special_factors["college"] }
          % endif
      - "childOneLivedWithFirstName": |
          % if len(children) > 0:
          ${ children[0].lived_with.name.first }
          % endif
      - "childOneLivedWithMiddleName": |
          % if len(children) > 0:
          ${ children[0].lived_with.name.middle }
          % endif
      - "childOneLivedWithLastName": |
          % if len(children) > 0:
          ${ children[0].lived_with.name.last }
          % endif
      - "childOneLivedWithSuffix": |
          % if len(children) > 0:
          ${ children[0].lived_with.name.suffix }
          % endif
      - "childOneLegalCustody1": |
          % if len(children) > 0:
          ${ children[0].legal_custody == "petitioner" }
          % endif
      - "childOneLegalCustody2": |
          % if len(children) > 0:
          ${ children[0].legal_custody == "respondent" }
          % endif
      - "childOneLegalCustody3": |
          % if len(children) > 0:
          ${ children[0].legal_custody == "Joint" }
          % endif
      - "childOneLegalCustody4": |
          % if len(children) > 0:
          ${ children[0].legal_custody == "Third Party" }
          % endif
      - "childOneLegalCustodyOtherPersonFirstName": |
          % if len(children) > 0:
          ${ legal_custody_other.name.first }
          % endif
      - "childOneLegalCustodyOtherPersonMiddleName": |
          % if len(children) > 0:
          ${ legal_custody_other.name.middle }
          % endif
      - "childOneLegalCustodyOtherPersonLastName": |
          % if len(children) > 0:
          ${ legal_custody_other.name.last }
          % endif
      - "childOneLegalCustodyOtherPersonSuffix": |
          % if len(children) > 0:
          ${ legal_custody_other.name.suffix }
          % endif
      - "childOnePhysicalCustody1": |
          % if len(children) > 0:
          ${ children[0].physical_custody == "petitioner" }
          % endif
      - "childOnePhysicalCustody2": |
          % if len(children) > 0:
          ${ children[0].physical_custody == "respondent" }
          % endif
      - "childOnePhysicalCustody3": |
          % if len(children) > 0:
          ${ children[0].physical_custody == "Joint" }
          % endif
      - "childOnePhysicalCustody4": |
          % if len(children) > 0:
          ${ children[0].physical_custody == "Third Party" }
          % endif
      - "childOnePhysicalCustodyOtherPersonMiddleName": |
          % if len(children) > 0:
          ${ physical_custody_other.name.middle }
          % endif
      - "childOnePhysicalCustodyOtherPersonLastName": |
          % if len(children) > 0:
          ${ physical_custody_other.name.last }
          % endif
      - "childOnePhysicalCustodySuffix": |
          % if len(children) > 0:
          ${ physical_custody_other.name.suffix }
          % endif
      - "childOnePhysicalCustodyOtherPersonFirstName": |
          % if len(children) > 0:
          ${ physical_custody_other.name.first }
          % endif
      - "childOneParentsMe": |
          % if len(children) > 0:
          ${ children[0].parents[users[0].name.full()] }
          % endif
      - "childOneParentsMySpouse": |
          % if len(children) > 0:
          ${ children[0].parents[other_parties[0].name.full()] }
          % endif
      - "childOneParentsOtherPerson": |
          % if len(children) > 0:
          ${ children[0].parents["Other"] }
          % endif
      - "childOneParentsOtherPersonFirstName": |
          % if len(children) > 0:
          ${ children[0].parent_other.name.first }
          % endif
      - "childOneParentsOtherPersonMiddleName": |
          % if len(children) > 0:
          ${ children[0].parent_other.name.middle }
          % endif
      - "childOneParentsOtherPersonLastName": |
          % if len(children) > 0:
          ${ children[0].parent_other.name.last }
          % endif
      - "childOneParentsOtherPersonSuffix": |
          % if len(children) > 0:
          ${ children[0].parent_other.name.suffix }
          % endif
      - "childOneBirthCertMe": |
          % if len(children) > 0:
          ${ children[0].birth_certificate[users[0].name.full()] }
          % endif
      - "childOneBirthCertSpouse": |
          % if len(children) > 0:
          ${ children[0].birth_certificate[other_parties[0].name.full()] }
          % endif
      - "childOneBirthCertOtherPerson": |
          % if len(children) > 0:
          ${ children[0].birth_certificate["Other"] }
          % endif
      - "childOneBirthCertOtherPersonFirstName": |
          % if len(children) > 0:
          ${ children[0].birth_certificate_other.name.first }
          % endif
      - "childOneBirthCertOtherPersonMiddleName": |
          % if len(children) > 0:
          ${ children[0].birth_certificate_other.name.middle }
          % endif
      - "childOneBirthCertOtherPersonLastName": |
          % if len(children) > 0:
          ${ children[0].birth_certificate_other.name.last }
          % endif
      - "childOneBirthCertOtherPersonSuffix": |
          % if len(children) > 0:
          ${ children[0].birth_certificate_other.name.suffix }
          % endif
      - "childTwoFirstName": |
          % if len(children) > 1:
          ${ redact(children[1].name.first) }
          % endif
      - "childTwoMiddleName": |
          % if len(children) > 1:
          ${ redact(children[1].name.middle) }
          % endif
      - "childTwoLastName": |
          % if len(children) > 1:
          ${ redact(children[1].name.last) }
          % endif
      - "childTwoSuffixJrSrIII": |
          % if len(children) > 1:
          ${ redact(children[1].name.suffix) }
          % endif
      - "childTwoSsnLastFour": |
          % if len(children) > 1:
          ${ redact("{:.0f}".format(children[1].ssn)) }
          % endif
      - "childTwoStreet": |
          % if len(children) > 1:
          ${ children[1].address.line_one() }
          % endif
      - "childTwoCity": |
          % if len(children) > 1:
          ${ children[1].address.city }
          % endif
      - "childTwoState": |
          % if len(children) > 1:
          ${ children[1].address.state }
          % endif
      - "childTwoZip": |
          % if len(children) > 1:
          ${ children[1].address.zip }
          % endif
      - "childTwoAge": |
          % if len(children) > 1:
          % if date_difference(starting=children[1].birthdate,ending=today()).years > 1:
          ${ "{:.0f}".format(date_difference(starting=children[1].birthdate,ending=today()).years) }
          % else:
          ${ date_difference(starting=children[1].birthdate, ending=today()).describe(specificity='month') }
          % endif
          % endif
      - "childTwoMarriedOrNot": |
          % if len(children) > 1:
          ${ children[1].special_factors["married"] }
          % endif
      - "childTwoActiveDutyOrNot": |
          % if len(children) > 1:
          ${ children[1].special_factors["active_duty"] }
          % endif
      - "childTwoSelfSupportingOrNot": |
          % if len(children) > 1:
          ${ children[1].special_factors["self_supporting"] }
          % endif
      - "childTwoAttendingHighSchoolOrNot": |
          % if len(children) > 1:
          ${ children[1].special_factors["high_school"] }
          % endif
      - "childTwoAttendingCollegeOrVocSchool": |
          % if len(children) > 1:
          ${ children[1].special_factors["college"] }
          % endif
      - "childTwoLivedWithFirstName": |
          % if len(children) > 1:
          ${ children[1].lived_with.name.first }
          % endif
      - "childTwoLivedWithMiddleName": |
          % if len(children) > 1:
          ${ children[1].lived_with.name.middle }
          % endif
      - "childTwoLivedWithLastName": |
          % if len(children) > 1:
          ${ children[1].lived_with.name.last }
          % endif
      - "childTwoLivedWithSuffix": |
          % if len(children) > 1:
          ${ children[1].lived_with.name.suffix }
          % endif
      - "childTwoLegalCustody1": |
          % if len(children) > 1:
          ${ children[1].legal_custody == "petitioner" }
          % endif
      - "childTwoLegalCustody2": |
          % if len(children) > 1:
          ${ children[1].legal_custody == "respondent" }
          % endif
      - "childTwoLegalCustody3": |
          % if len(children) > 1:
          ${ children[1].legal_custody == "Joint" }
          % endif
      - "childTwoLegalCustody4": |
          % if len(children) > 1:
          ${ children[1].legal_custody == "Third Party" }
          % endif
      - "childTwoLegalCustodyOtherPersonMiddleName": |
          % if len(children) > 1:
          ${ legal_custody_other.name.first }
          % endif
      - "childTwoLegalCustodyOtherPersonSuffix": |
          % if len(children) > 1:
          ${ legal_custody_other.name.middle }
          % endif
      - "childTwoLegalCustodyOtherPersonFirstName": |
          % if len(children) > 1:
          ${ legal_custody_other.name.last }
          % endif
      - "childTwoLegalCustodyOtherPersonLastName": |
          % if len(children) > 1:
          ${ legal_custody_other.name.suffix }
          % endif
      - "childTwoPhysicalCustody1": |
          % if len(children) > 1:
          ${ children[1].physical_custody == "petitioner" }
          % endif
      - "childTwoPhysicalCustody2": |
          % if len(children) > 1:
          ${ children[1].physical_custody == "respondent" }
          % endif
      - "childTwoPhysicalCustody3": |
          % if len(children) > 1:
          ${ children[1].physical_custody == "Joint" }
          % endif
      - "childTwoPhysicalCustody4": |
          % if len(children) > 1:
          ${ children[1].physical_custody == "Third Party" }
          % endif
      - "childTwoPhysicalCustodyOtherPersonFirstName": |
          % if len(children) > 1:
          ${ physical_custody_other.name.middle }
          % endif
      - "childTwoPhysicalCustodyOtherPersonMiddleName": |
          % if len(children) > 1:
          ${ physical_custody_other.name.last }
          % endif
      - "childTwoPhysicalCustodyOtherPersonLastName": |
          % if len(children) > 1:
          ${ physical_custody_other.name.suffix }
          % endif
      - "childTwoPhysicalCustodyOtherPersonSuffix": |
          % if len(children) > 1:
          ${ physical_custody_other.name.first }
          % endif
      - "childTwoParentsMe": |
          % if len(children) > 1:
          ${ children[1].parents[users[0].name.full()] }
          % endif
      - "childTwoParentsMySpouse": |
          % if len(children) > 1:
          ${ children[1].parents[other_parties[0].name.full()] }
          % endif
      - "childTwoParentsOtherPerson": |
          % if len(children) > 1:
          ${ children[1].parents["Other"] }
          % endif
      - "childTwoParentsOtherPersonFirstName": |
          % if len(children) > 1:
          ${ children[1].parent_other.name.first }
          % endif
      - "childTwoParentsOtherPersonMiddleName": |
          % if len(children) > 1:
          ${ children[1].parent_other.name.middle }
          % endif
      - "childTwoParentsOtherPersonLastName": |
          % if len(children) > 1:
          ${ children[1].parent_other.name.last }
          % endif
      - "childTwoParentsOtherPersonSuffix": |
          % if len(children) > 1:
          ${ children[1].parent_other.name.suffix }
          % endif
      - "childTwoBirthCertMe": |
          % if len(children) > 1:
          ${ children[1].birth_certificate[users[0].name.full()] }
          % endif
      - "childTwoBirthCertSpouse": |
          % if len(children) > 1:
          ${ children[1].birth_certificate[other_parties[0].name.full()] }
          % endif
      - "childTwoBirthCertOtherPerson": |
          % if len(children) > 1:
          ${ children[1].birth_certificate["Other"] }
          % endif
      - "childTwoBirthCertOtherPersonFirstName": |
          % if len(children) > 1:
          ${ children[1].birth_certificate_other.name.first }
          % endif
      - "childTwoBirthCertOtherPersonLastName": |
          % if len(children) > 1:
          ${ children[1].birth_certificate_other.name.last }
          % endif
      - "childTwoBirthCertOtherPersonMiddleName": |
          % if len(children) > 1:
          ${ children[1].birth_certificate_other.name.middle }
          % endif
      - "childTwoBirthCertOtherPersonSuffix": |
          % if len(children) > 1:
          ${ children[1].birth_certificate_other.name.suffix }
          % endif
      - "childThreeFirstName": |
          % if len(children) > 2:
          ${ redact(children[2].name.first) }
          % endif
      - "childThreeMiddleName": |
          % if len(children) > 2:
          ${ redact(children[2].name.middle) }
          % endif
      - "childThreeLastName": |
          % if len(children) > 2:
          ${ redact(children[2].name.last) }
          % endif
      - "childThreeSuffixJrSrIII": |
          % if len(children) > 2:
          ${ redact(children[2].name.suffix) }
          % endif
      - "childThreeSsnLastFour": |
          % if len(children) > 2:
          ${ redact("{:.0f}".format(children[2].ssn)) }
          % endif
      - "childThreeStreet": |
          % if len(children) > 2:
          ${ children[2].address.line_one() }
          % endif
      - "childThreeCity": |
          % if len(children) > 2:
          ${ children[2].address.city }
          % endif
      - "childThreeState": |
          % if len(children) > 2:
          ${ children[2].address.state }
          % endif
      - "childThreeZip": |
          % if len(children) > 2:
          ${ children[2].address.zip }
          % endif
      - "childThreeAge": |
          % if len(children) > 2:
          % if date_difference(starting=children[2].birthdate,ending=today()).years > 1:
          ${ "{:.0f}".format(date_difference(starting=children[2].birthdate,ending=today()).years) }
          % else:
          ${ date_difference(starting=children[2].birthdate, ending=today()).describe(specificity='month') }
          % endif
          % endif
      - "childThreeMarriedOrNot": |
          % if len(children) > 2:
          ${ children[2].special_factors["married"] }
          % endif
      - "childThreeActiveDutyOrNot": |
          % if len(children) > 2:
          ${ children[2].special_factors["active_duty"] }
          % endif
      - "childThreeSelfSupportingOrNot": |
          % if len(children) > 2:
          ${ children[2].special_factors["self_supporting"] }
          % endif
      - "childThreeAttendingHighSchoolOrNot": |
          % if len(children) > 2:
          ${ children[2].special_factors["high_school"] }
          % endif
      - "childThreeAttendingCollegeOrVocSchool": |
          % if len(children) > 2:
          ${ children[2].special_factors["college"] }
          % endif
      - "childThreeLivedWithFirstName": |
          % if len(children) > 2:
          ${ children[2].lived_with.name.first }
          % endif
      - "childThreeLivedWithMiddleName": |
          % if len(children) > 2:
          ${ children[2].lived_with.name.middle }
          % endif
      - "childThreeLivedWithLastName": |
          % if len(children) > 2:
          ${ children[2].lived_with.name.last }
          % endif
      - "childThreeLivedWithSuffix": |
          % if len(children) > 2:
          ${ children[2].lived_with.name.suffix }
          % endif
      - "childThreeLegalCustody1": |
          % if len(children) > 2:
          ${ children[2].legal_custody == "petitioner" }
          % endif
      - "childThreeLegalCustody2": |
          % if len(children) > 2:
          ${ children[2].legal_custody == "respondent" }
          % endif
      - "childThreeLegalCustody3": |
          % if len(children) > 2:
          ${ children[2].legal_custody == "Joint" }
          % endif
      - "childThreeLegalCustody4": |
          % if len(children) > 2:
          ${ children[2].legal_custody == "Third Party" }
          % endif
      - "childThreeLegalCustodyOtherPersonFirstName": |
          % if len(children) > 2:
          ${ legal_custody_other.name.first }
          % endif
      - "childThreeLegalCustodyOtherPersonMiddleName": |
          % if len(children) > 2:
          ${ legal_custody_other.name.middle }
          % endif
      - "childThreeLegalCustodyOtherPersonLastName": |
          % if len(children) > 2:
          ${ legal_custody_other.name.last }
          % endif
      - "childThreeLegalCustodyOtherPersonSuffix": |
          % if len(children) > 2:
          ${ legal_custody_other.name.suffix }
          % endif
      - "childThreePhysicalCustody1": |
          % if len(children) > 2:
          ${ children[2].physical_custody == "petitioner" }
          % endif
      - "childThreePhysicalCustody2": |
          % if len(children) > 2:
          ${ children[2].physical_custody == "respondent" }
          % endif
      - "childThreePhysicalCustody3": |
          % if len(children) > 2:
          ${ children[2].physical_custody == "Joint" }
          % endif
      - "childThreePhysicalCustody4": |
          % if len(children) > 2:
          ${ children[2].physical_custody == "Third Party" }
          % endif
      - "childThreePhysicalCustodyOtherPersonMiddleName": |
          % if len(children) > 2:
          ${ physical_custody_other.name.middle }
          % endif
      - "childThreePhysicalCustodyOtherPersonLastName": |
          % if len(children) > 2:
          ${ physical_custody_other.name.last }
          % endif
      - "childThreePhysicalCustodyOtherPersonSuffix": |
          % if len(children) > 2:
          ${ physical_custody_other.name.suffix }
          % endif
      - "childThreePhysicalCustodyOtherPersonFirstName": |
          % if len(children) > 2:
          ${ physical_custody_other.name.first }
          % endif
      - "childThreeParentsMe": |
          % if len(children) > 2:
          ${ children[2].parents[users[0].name.full()] }
          % endif
      - "childThreeParentsMySpouse": |
          % if len(children) > 2:
          ${ children[2].parents[other_parties[0].name.full()] }
          % endif
      - "childThreeParentsOtherPerson": |
          % if len(children) > 2:
          ${ children[2].parents["Other"] }
          % endif
      - "childThreeParentsOtherPersonFirstName": |
          % if len(children) > 2:
          ${ children[2].parent_other.name.first }
          % endif
      - "childThreeParentsOtherPersonMiddleName": |
          % if len(children) > 2:
          ${ children[2].parent_other.name.middle }
          % endif
      - "childThreeParentsOtherPersonLastName": |
          % if len(children) > 2:
          ${ children[2].parent_other.name.last }
          % endif
      - "childThreeParentsOtherPersonSuffix": |
          % if len(children) > 2:
          ${ children[2].parent_other.name.suffix }
          % endif
      - "childThreeBirthCertMe": |
          % if len(children) > 2:
          ${ children[2].birth_certificate[users[0].name.full()] }
          % endif
      - "childThreeBirthCertSpouse": |
          % if len(children) > 2:
          ${ children[2].birth_certificate[other_parties[0].name.full()] }
          % endif
      - "childThreeBirthCertOtherPerson": |
          % if len(children) > 2:
          ${ children[2].birth_certificate["Other"] }
          % endif
      - "childThreeBirthCertOtherPersonFirstName": |
          % if len(children) > 2:
          ${ children[2].birth_certificate_other.name.first }
          % endif
      - "childThreeBirthCertOtherPersonMiddleName": |
          % if len(children) > 2:
          ${ children[2].birth_certificate_other.name.middle }
          % endif
      - "childThreeBirthCertOtherPersonLastName": |
          % if len(children) > 2:
          ${ children[2].birth_certificate_other.name.last }
          % endif
      - "childThreeBirthCertOtherPersonSuffix": |
          % if len(children) > 2:
          ${ children[2].birth_certificate_other.name.suffix }
          % endif
      - "childFourMiddleName": |
          % if len(children) > 3:
          ${ redact(children[3].name.middle) }
          % endif
      - "childFourFirstName": |
          % if len(children) > 3:
          ${ redact(children[3].name.first) }
          % endif
      - "childFourLastName": |
          % if len(children) > 3:
          ${ redact(children[3].name.last) }
          % endif
      - "childFourSuffixJrSrIII": |
          % if len(children) > 3:
          ${ redact(children[3].name.suffix) }
          % endif
      - "childFourSsnLastFour": |
          % if len(children) > 3:
          ${ redact("{:.0f}".format(children[3].ssn)) }
          % endif
      - "childFourStreet": |
          % if len(children) > 3:
          ${ children[3].address.line_one() }
          % endif
      - "childFourCity": |
          % if len(children) > 3:
          ${ children[3].address.city }
          % endif
      - "childFourState": |
          % if len(children) > 3:
          ${ children[3].address.state }
          % endif
      - "childFourZip": |
          % if len(children) > 3:
          ${ children[3].address.zip }
          % endif
      - "childFourAge": |
          % if len(children) > 3:
          % if date_difference(starting=children[3].birthdate,ending=today()).years > 1:
          ${ "{:.0f}".format(date_difference(starting=children[3].birthdate,ending=today()).years) }
          % else:
          ${ date_difference(starting=children[3].birthdate, ending=today()).describe(specificity='month') }
          % endif
          % endif
      - "childFourMarriedOrNot": |
          % if len(children) > 3:
          ${ children[3].special_factors["married"] }
          % endif
      - "childFourActiveDutyOrNot": |
          % if len(children) > 3:
          ${ children[3].special_factors["active_duty"] }
          % endif
      - "childFourSelfSupportingOrNot": |
          % if len(children) > 3:
          ${ children[3].special_factors["self_supporting"] }
          % endif
      - "childFourAttendingHighSchoolOrNot": |
          % if len(children) > 3:
          ${ children[3].special_factors["high_school"] }
          % endif
      - "childFourAttendingCollegeOrVocSchool": |
          % if len(children) > 3:
          ${ children[3].special_factors["college"] }
          % endif
      - "childFourLivedWithFirstName": |
          % if len(children) > 3:
          ${ children[3].lived_with.name.first }
          % endif
      - "childFourLivedWithMiddleName": |
          % if len(children) > 3:
          ${ children[3].lived_with.name.middle }
          % endif
      - "childFourLivedWithLastName": |
          % if len(children) > 3:
          ${ children[3].lived_with.name.last }
          % endif
      - "childFourLivedWithSuffix": |
          % if len(children) > 3:
          ${ children[3].lived_with.name.suffix }
          % endif
      - "childFourLegalCustody1": |
          % if len(children) > 3:
          ${ children[3].legal_custody == "petitioner" }
          % endif
      - "childFourLegalCustody2": |
          % if len(children) > 3:
          ${ children[3].legal_custody == "respondent" }
          % endif
      - "childFourLegalCustody3": |
          % if len(children) > 3:
          ${ children[3].legal_custody == "Joint" }
          % endif
      - "childFourLegalCustody4": |
          % if len(children) > 3:
          ${ children[3].legal_custody == "Third Party" }
          % endif
      - "childFourLegalCustodyOtherPersonFirstName": |
          % if len(children) > 3:
          ${ legal_custody_other.name.first }
          % endif
      - "childFourLegalCustodyOtherPersonMiddleName": |
          % if len(children) > 3:
          ${ legal_custody_other.name.middle }
          % endif
      - "childFourLegalCustodyOtherPersonLastName": |
          % if len(children) > 3:
          ${ legal_custody_other.name.last }
          % endif
      - "childFourLegalCustodyOtherPersonSuffix": |
          % if len(children) > 3:
          ${ legal_custody_other.name.suffix }
          % endif
      - "childFourPhysicalCustody1": |
          % if len(children) > 3:
          ${ children[3].physical_custody == "petitioner" }
          % endif
      - "childFourPhysicalCustody2": |
          % if len(children) > 3:
          ${ children[3].physical_custody == "respondent" }
          % endif
      - "childFourPhysicalCustody3": |
          % if len(children) > 3:
          ${ children[3].physical_custody == "Joint" }
          % endif
      - "childFourPhysicalCustody4": |
          % if len(children) > 3:
          ${ children[3].physical_custody == "Third Party" }
          % endif
      - "childFourPhysicalCustodyOtherPersonMiddleName": |
          % if len(children) > 3:
          ${ physical_custody_other.name.middle }
          % endif
      - "childFourPhysicalCustodyOtherPersonLastName": |
          % if len(children) > 3:
          ${ physical_custody_other.name.last }
          % endif
      - "childFourPhysicalCustodyOtherPersonSuffix": |
          % if len(children) > 3:
          ${ physical_custody_other.name.suffix }
          % endif
      - "childFourPhysicalCustodyOtherPersonFirstName": |
          % if len(children) > 3:
          ${ physical_custody_other.name.first }
          % endif
      - "childFourParentsMe": |
          % if len(children) > 3:
          ${ children[3].parents[users[0].name.full()] }
          % endif
      - "childFourParentsMySpouse": |
          % if len(children) > 3:
          ${ children[3].parents[other_parties[0].name.full()] }
          % endif
      - "childFourParentsOtherPerson": |
          % if len(children) > 3:
          ${ children[3].parents["Other"] }
          % endif
      - "childFourParentsOtherPersonFirstName": |
          % if len(children) > 3:
          ${ children[3].parent_other.name.first }
          % endif
      - "childFourParentsOtherPersonMiddleName": |
          % if len(children) > 3:
          ${ children[3].parent_other.name.middle }
          % endif
      - "childFourParentsOtherPersonLastName": |
          % if len(children) > 3:
          ${ children[3].parent_other.name.last }
          % endif
      - "childFourParentsOtherPersonSuffix": |
          % if len(children) > 3:
          ${ children[3].parent_other.name.suffix }
          % endif
      - "childFourBirthCertMe": |
          % if len(children) > 3:
          ${ children[3].birth_certificate[users[0].name.full()] }
          % endif
      - "childFourBirthCertSpouse": |
          % if len(children) > 3:
          ${ children[3].birth_certificate[other_parties[0].name.full()] }
          % endif
      - "childFourBirthCertOtherPerson": |
          % if len(children) > 3:
          ${ children[3].birth_certificate["Other"] }
          % endif
      - "childFourBirthCertOtherPersonFirstName": |
          % if len(children) > 3:
          ${ children[3].birth_certificate_other.name.first }
          % endif
      - "childFourBirthCertOtherPersonMiddleName": |
          % if len(children) > 3:
          ${ children[3].birth_certificate_other.name.middle }
          % endif
      - "childFourBirthCertOtherPersonLastName": |
          % if len(children) > 3:
          ${ children[3].birth_certificate_other.name.last }
          % endif
      - "childFourBirthCertOtherPersonSuffix": |
          % if len(children) > 3:
          ${ children[3].birth_certificate_other.name.suffix }
          % endif
      - "person1ChildrenLivedWithPast5YearsSuffix": ${ persons_lived_with[0].name.suffix } 
      - "person1ChildrenLivedWithPast5YearsFirstName": ${ persons_lived_with[0].name.first } 
      - "person1ChildrenLivedWithPast5YearsMiddleName": ${ persons_lived_with[0].name.middle } 
      - "person1ChildrenLivedWithPast5YearsLastName": ${ persons_lived_with[0].name.last } 
      - "person1ChildrenLivedWithPast5YearsStreet": ${ persons_lived_with[0].address.line_one() } 
      - "person1ChildrenLivedWithPast5YearsCity": ${ persons_lived_with[0].address.city } 
      - "person1ChildrenLivedWithPast5YearsState": ${ persons_lived_with[0].address.state } 
      - "person1ChildrenLivedWithPast5YearsZip": ${ persons_lived_with[0].address.zip } 
      - "person2ChildrenLivedWithPast5YearsFirstName": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].name.first } 
          % endif
      - "person2ChildrenLivedWithPast5YearsMiddleName": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].name.middle } 
          % endif
      - "person2ChildrenLivedWithPast5YearsLastName": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].name.last } 
          % endif
      - "person2ChildrenLivedWithPast5YearsSuffix": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].name.suffix } 
          % endif
      - "person2ChildrenLivedWithPast5YearsStreet": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].address.line_one() } 
          % endif
      - "person2ChildrenLivedWithPast5YearsCity": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].address.city } 
          % endif 
      - "person2ChildrenLivedWithPast5YearsState": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].address.state }  
          % endif
      - "person2ChildrenLivedWithPast5YearsZip": |
          % if len(persons_lived_with) > 1:
          ${ persons_lived_with[1].address.zip }  
          % endif
      - "person3ChildrenLivedWithPast5YearsFirstName": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].name.first }  
          % endif
      - "person3ChildrenLivedWithPast5YearsMiddleName": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].name.middle }  
          % endif
      - "person3ChildrenLivedWithPast5YearsLastName": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].name.last }  
          % endif
      - "person3ChildrenLivedWithPast5YearsSuffix": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].name.suffix }  
          % endif
      - "person3ChildrenLivedWithPast5YearsStreet": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].address.line_one() }  
          % endif
      - "person3ChildrenLivedWithPast5YearsZip": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].address.zip }  
          % endif
      - "person3ChildrenLivedWithPast5YearsCity": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].address.city }  
          % endif
      - "person3ChildrenLivedWithPast5YearsState": |
          % if len(persons_lived_with) > 2:
          ${ persons_lived_with[2].address.state }  
          % endif
      - "doesAnyoneOtherThanYouOrSpouseHaveCustodyRights1": ${ others_with_physical_custody } 
      - "doesAnyoneOtherThanYouOrSpouseHaveCustodyRights2": ${ not others_with_physical_custody } 
      - "otherCustodyProceedingPending1": ${ other_custody_proceeding } 
      - "otherCustodyProceedingPending2": ${ not other_custody_proceeding } 
      - "participatedInOtherLitigationCustody1": ${ other_litigation } 
      - "participatedInOtherLitigationCustody2": ${ not other_litigation } 
      - "haveChildrenBeenVictimAbuseOrNeglect1": ${ abuse_or_neglect } 
      - "haveChildrenBeenVictimAbuseOrNeglect2": ${ not abuse_or_neglect } 
      - "explanationIfAnsweredYesToFortyThroughFortyThree": ${ children_explanation } 
      - "anyOrdersEnteredByFSD1": ${ family_support_order } 
      - "anyOrdersEnteredByFSD2": ${ not family_support_order } 
      - "areYouOrSpouseReceivingTanf1": ${ len(petitioner.benefits.matches(source=['tanf'])) > 0 or len(respondent.benefits.matches(source=['tanf'])) > 0 } 
      - "areYouOrSpouseReceivingTanf2": ${ len(petitioner.benefits.matches(source=['tanf'])) == 0 and len(respondent.benefits.matches(source=['tanf'])) == 0 } 
      - "otherAllegations": ${ other_allegations } 
      - "requestReliefDissolution": ${ True } 
      - "requestReliefCustody": ${ relief['custody'] } 
      - "requestReliefSupportOfTheChildren": ${ relief['child_support'] } 
      - "requestReliefDivideMaritalPropertyAndDebts": ${ relief['divide_property'] } 
      - "requestReliefAwardMaintenanceToMe": ${ relief['maintenance'] and maintenance_awarded_to == "petitioner" } 
      - "requestReliefAwardMaintenanceToSpouse": ${ relief['maintenance'] and maintenance_awarded_to == "respondent" } 
      - "requestReliefChangeName": ${ relief['change_name'] } 
      - "requestReliefFormerFirstName": ${ users[0].prior_name.name.first if relief['change_name'] else "" } 
      - "requestReliefFormerMiddleName": ${ users[0].prior_name.name.middle if relief['change_name'] else "" } 
      - "requestReliefFormerLastName": ${ users[0].prior_name.name.last if relief['change_name'] else "" } 
      - "requestReliefFormerSuffix": ${ users[0].prior_name.name.suffix if relief['change_name'] else "" } 
      - "requestReliefOther": ${ relief['other'] } 
      - "requestReliefOtherRequests": ${ relief_other } 
      - "serviceOnSpouse1": ${ other_parties[0].service_type == "none" } 
      - "serviceOnSpouse2": ${ other_parties[0].service_type == "house" } 
      - "serviceOnSpouseStreet": ${ respondent.service_address.line_one() if other_parties[0].service_type == "house" else "" } 
      - "serviceOnSpouseCity": ${ respondent.service_address.city if other_parties[0].service_type == "house" else ""  } 
      - "serviceOnSpouseZip": ${ respondent.service_address.zip if other_parties[0].service_type == "house" else "" } 
      - "serviceOnSpouseState": ${ respondent.service_address.state if other_parties[0].service_type == "house" else "" } 
      - "serviceOnSpouse3": ${ other_parties[0].service_type == "work" } 
      - "serviceOnSpouseEmployersName": ${ respondent.employer.name.text if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouseHoursOfEmployment": ${ respondent.hours_of_employment if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouseEmployersStreet": ${ respondent.service_address.line_one()if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouseEmployersCity": ${ respondent.service_address.city if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouseEmployersZip": ${ respondent.service_address.zip if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouseEmployersState": ${ respondent.service_address.state if other_parties[0].service_type == "work" else "" } 
      - "serviceOnSpouse4": ${ other_parties[0].service_type == "mail" } 
      - "ifYouOrSpouseReceiveTanf": ${ len(petitioner.benefits.matches(source=['tanf'])) > 0 or len(respondent.benefits.matches(source=['tanf'])) > 0  } 
      - "petitionersAttorneycCheckbox": ${ petitioner.attorney.assistance } 
      - "petitionersAttorneysBarNumber": ${ petitioner.attorney.bar_number } 
      - "petitionersAttorneyPrintName": ${ petitioner.attorney.name_full() } 
      - "petitionersAttorneyStreet": ${ petitioner.attorney.address.line_one() } 
      - "petitionersAttorneyCity": ${ petitioner.attorney.address.state } 
      - "petitionersAttorneyState": ${ petitioner.attorney.address.city } 
      - "petitionersAttorneyZip": ${ petitioner.attorney.address.zip } 
      - "petitionersAttorneyPhone": ${ phone_number_formatted(petitioner.attorney.phone_number) } 
      - "petitionersAttorneyFaxNumber": ${ "" } 
      - "petitionersAttorneyEmail": ${ petitioner.attorney.email } 
---
